image: erlang

before_script:
  - apt-get update
  - apt-get -y install lsb-release ruby ruby-dev
  - gem install package_cloud
  - gem install fpm

stages:
  - build

app:
  stage: build
  script:
    - make
    - bin/package
  only:
    - master
